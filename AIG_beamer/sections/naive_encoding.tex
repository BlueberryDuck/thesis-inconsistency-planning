\section{Why naive encoding fails}

% BUDGET: ~90s
\begin{frame}{A propositional encoding of the drill chain works\dots}
    The goal is asserted as a fact, then preconditions are traced back until the chain hits a proposition no operator produces.

    \begin{columns}[T]
        \begin{column}{0.6\textwidth}
            \begin{exampleblock}{Precondition encoding $K_{\text{drill}}$}
                \footnotesize
                $K_{\text{drill}} = \{\ \text{soil\_sampled},$ \hfill {\scriptsize\textcolor{gray}{goal}} \\[2pt]
                $\hphantom{K_{\text{drill}} = \{\ } \text{soil\_sampled} \rightarrow \text{hole\_drilled},$ \hfill {\scriptsize\textcolor{gray}{sample precond.}} \\[2pt]
                $\hphantom{K_{\text{drill}} = \{\ } \text{hole\_drilled} \rightarrow \text{arm\_deployed} \land \text{drill\_bit},$ \hfill {\scriptsize\textcolor{gray}{drill precond.}} \\[2pt]
                $\hphantom{K_{\text{drill}} = \{\ } \neg \text{drill\_bit}\ \}$ \hfill {\scriptsize\textcolor{gray}{closed world assumption}}
            \end{exampleblock}
        \end{column}
        \begin{column}{0.4\textwidth}
            \vspace*{-0.2em}
            \centering
            \input{figures/fig_rover_drill}
        \end{column}
    \end{columns}

    \begin{block}{Contension measure $\mathcal{I}_c$}
        \begin{itemize}
            \item $\neg\textit{drill\_bit}$ blocks the chain, forcing $v(\textit{drill\_bit}) = B$, so $\mathcal{I}_c(K_{\text{drill}}) = 1$
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S4.2}
\end{frame}

% BUDGET: ~90s
\begin{frame}{\dots\ but either misses the conflict or flattens it to $\mathcal{I}_c = 1$}
    \begin{exampleblock}{$\mathcal{I}_c$ under two encodings, neither informative}
        \centering
        \begin{tabular}{lcc}
            \toprule
            Planning failure                        & precondition & delete-enriched \\
            \midrule
            Goal with no producer                   & $1$          & $1$             \\
            Two goals that never hold together      & $0$          & $1$             \\
            A goal that irreversibly blocks another & $0$          & $1$             \\
            \bottomrule
        \end{tabular}
    \end{exampleblock}

    \begin{block}{Why neither helps}
        \begin{itemize}
            \item Without delete effects, operator-induced conflicts read as a false $0$
            \item With them, $B$ collapses each conflict to a flat $1$
            \item Structural information about the conflict is lost, so $\mathcal{I}_c$ cannot distinguish between them
        \end{itemize}
    \end{block}

    \sectioncitebk{bk:enriched}{Thesis \S4.2}
\end{frame}
