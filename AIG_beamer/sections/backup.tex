\appendix

\section*{Backup}

\begin{frame}[noframenumbering,plain]
    \vfill
    \begin{center}
        {\Huge\bfseries Backup slides}\\[1em]
        {\large Reachability, proofs, postulates, performance, scenarios}
    \end{center}
    \vfill
\end{frame}

%========================================================================
% Motivation / Foundations -- related work (Thesis \S1, \S3)
%========================================================================

\begin{frame}[noframenumbering]{Backup, comparison vs Eriksson and Goebelbecker}
    \hypertarget{bk:eriksson-goebel}{}
    \begin{center}
        \scriptsize
        \begin{tabular}{lll}
            \toprule
            Approach                                        & Output                                                                                & Diagnostic shape         \\
            \midrule
            Dead-state certificates [Eriksson et al., 2018] & solvable or unsolvable                                                                & binary, no severity      \\
            Excuses [G\"obelbecker et al., 2010]            & minimal repair edits                                                                  & prescriptive             \\
            Explanations [Sreedharan et al., 2019]          & unreachable-goal accounts                                                             & qualitative, per case    \\
            This thesis                                     & profile $(\mathcal{I}_{\text{UR}}, \mathcal{I}_{\text{MX}}, \mathcal{I}_{\text{GS}})$ & quantitative, structural \\
            \bottomrule
        \end{tabular}
    \end{center}

    \begin{block}{Complementary roles}
        \begin{itemize}
            \item $\mathcal{I}_{\text{GS}}$ recovers a quantitative form of dead-state analysis
            \item $\mathcal{I}_{\text{UR}}$ flags where add-producer excuses apply, $\mathcal{I}_{\text{GS}}$ where add-reversal excuses do
            \item Goal-level mutex over $S^h_{\text{reach}}$ differs from Graphplan's level-based proposition mutex [Blum and Furst, 1997]
            \item Measures stay descriptive, leaving repair decisions to domain experts
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S1.1, \S3}
\end{frame}

%========================================================================
% Limitations of a propositional encoding (Thesis \S4.2)
%========================================================================

\begin{frame}[noframenumbering]{Backup, enriching the encoding still scores $1$}
    \hypertarget{bk:enriched}{}
    \begin{exampleblock}{The Rover dish toggle under two encodings}
        \footnotesize
        Precondition-only \quad $K_{\text{dish}} = \{\text{aimed\_earth},\ \text{aimed\_probe},\ \text{aimed\_probe} \rightarrow \text{aimed\_earth},\ \text{aimed\_earth} \rightarrow \text{aimed\_probe}\}$\\[4pt]
        Enriched \quad $K^+_{\text{dish}} = K_{\text{dish}} \cup \{\text{aimed\_probe} \rightarrow \neg\text{aimed\_earth},\ \text{aimed\_earth} \rightarrow \neg\text{aimed\_probe}\}$
    \end{exampleblock}

    \begin{block}{Detection without information}
        \begin{itemize}
            \item Precondition-only is classically consistent, $v(\text{aimed\_earth}) = v(\text{aimed\_probe}) = T$, the mutex is invisible and $\mathcal{I}_c = 0$
            \item The delete-effect rule $p \rightarrow \neg d$ makes it inconsistent, so the mutex is now represented
            \item Yet $v(\text{aimed\_probe}) = B$, $v(\text{aimed\_earth}) = T$ satisfies every formula, so $\mathcal{I}_c = 1$
            \item $B$ absorbs the conflict at one proposition, the same flat score as the unreachable drill chain
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S4.2}
\end{frame}

%========================================================================
% Taxonomy of unsolvable planning problems (Thesis \S4.1)
%========================================================================

\begin{frame}[noframenumbering]{Backup, one problem, three structural categories}
    \hypertarget{bk:overlap}{}
    \begin{exampleblock}{The Rover Mission carries one failure per cluster}
        $G = \{\text{soil\_sampled}, \text{aimed\_earth}, \text{aimed\_probe}, \text{in\_orbit}, \text{mapped}\}$\\[3pt]
        Three independent subsystems contribute a structurally different failure to the same goal set.
    \end{exampleblock}

    \begin{block}{Three categories in one problem}
        \begin{itemize}
            \item Category 2a, the drill chain, no operator produces \textit{drill\_bit}, so \textit{soil\_sampled} is unreachable
            \item Category 1, the dish toggle, \textit{aimed\_earth} and \textit{aimed\_probe} are operator-induced reversible mutex
            \item Category 2c, the orbit chain, \textit{ascend} deletes \textit{mapped} with no way to restore it
            \item The orbit chain is at once a mutex and a sequencing conflict, so the categories overlap rather than partition
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S4.1}
\end{frame}

%========================================================================
% Planning-native measures -- scenarios and normalization (Thesis \S4.3, \S4.4, \S4.6)
%========================================================================

\begin{frame}[noframenumbering]{Backup, Bank Vault deepens the unreachability cascade}
    \hypertarget{bk:bank-vault}{}
    \begin{exampleblock}{Two unreachability profiles at convergence}
        \centering
        \begin{tabular}{lccc}
            \toprule
            Scenario    & $|P|$ & $\mathcal{I}_{\text{UR}}(\Pi)$ & cascade depth \\
            \midrule
            Rover drill & 4     & $(1, 3)$                       & 2             \\
            Bank Vault  & 8     & $(1, 7)$                       & 6             \\
            \bottomrule
        \end{tabular}
    \end{exampleblock}

    \begin{block}{The drill chain is the shallow case, Bank Vault the deep one}
        \begin{itemize}
            \item The drill chain has one missing root \textit{drill\_bit}, blocking \textit{hole\_drilled} then \textit{soil\_sampled}, scope 1 over structure 3
            \item Bank Vault has three roots \textit{key1}, \textit{key2}, \textit{admin\_appr} with no producers, three blocked chains converging on \textit{in\_vault}
            \item Same scope value, the structure count grows with the number and length of converging blocked chains
            \item $\mathcal{I}^{\text{struct}}_{\text{UR}}$ separates a single short cascade from a deep multi-root one
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S4.3.1 Scenario~2}
\end{frame}

\begin{frame}[noframenumbering]{Backup, the orbit chain sequencing in full}
    \hypertarget{bk:trust-travel}{}
    \begin{exampleblock}{Why $(\text{in\_orbit}, \text{mapped})$ is one-directional}
        \small
        \textit{map} needs \textit{on\_surface} and \textit{comms}, \textit{ascend} reaches \textit{airborne} but deletes \textit{comms} and \textit{mapped}, \textit{deploy} then reaches \textit{in\_orbit}.
    \end{exampleblock}

    \begin{block}{Mapped before orbit works, orbit before mapped does not}
        \begin{itemize}
            \item \textit{mapped} is reachable from the initial state, and \textit{in\_orbit} is reachable after \textit{ascend} then \textit{deploy}
            \item Achieving \textit{mapped} first then ascending leaves a trace with \textit{mapped} at an earlier step than \textit{in\_orbit}, so that order is achievable
            \item Once \textit{in\_orbit} holds, \textit{comms} is gone, so \textit{map} can never fire again and \textit{mapped} is unreachable afterwards
            \item Only the ordered pair $(\text{in\_orbit}, \text{mapped})$ fails, giving $\mathcal{I}_{\text{GS}}(\Pi) = (2, 1)$
        \end{itemize}
    \end{block}

    \sectioncite{Constructed running example}
\end{frame}

\begin{frame}[noframenumbering]{Backup, symmetric sequencing raises the structure count}
    \hypertarget{bk:rival-alliances}{}
    \begin{exampleblock}{Rival Alliances, both orders fail}
        \[
            S_{\text{reach}} = \{\{\text{neutral\_A}, \text{neutral\_B}\},\ \{\text{neutral\_A}, \text{allied\_A}\},\ \{\text{neutral\_B}, \text{allied\_B}\}\}
        \]
        $G = \{\text{allied\_A}, \text{allied\_B}\}$, each \textit{ally} operator deletes the rival's neutral precondition irreversibly.
    \end{exampleblock}

    \begin{block}{Structure value separates symmetric from one-directional}
        \begin{itemize}
            \item The Rover orbit chain is one-directional, only $(\text{in\_orbit}, \text{mapped})$ fails, $\mathcal{I}_{\text{GS}} = (2, 1)$
            \item Rival Alliances fails both ways, $(\text{allied\_A}, \text{allied\_B})$ and $(\text{allied\_B}, \text{allied\_A})$, $\mathcal{I}_{\text{GS}} = (2, 2)$
            \item Same scope of two affected goals, the structure count distinguishes one blocked order from two
            \item Symmetric exclusion signals a harder repair, neither goal can be deferred to rescue the other
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S4.3.3 Scenario~6}
\end{frame}

\begin{frame}[noframenumbering]{Backup, the isolated scenarios behind the running example}
    \hypertarget{bk:signatures}{}
    \begin{center}
        \footnotesize
        \begin{tabular}{l cc cc cc}
            \toprule
            Scenario         & \multicolumn{2}{c}{$\mathcal{I}_{\text{UR}}$} & \multicolumn{2}{c}{$\mathcal{I}_{\text{MX}}$} & \multicolumn{2}{c}{$\mathcal{I}_{\text{GS}}$}                           \\
            \cmidrule(lr){2-3}\cmidrule(lr){4-5}\cmidrule(lr){6-7}
                             & scope                                         & struct                                        & scope                                         & struct & scope & struct \\
            \midrule
            Locked Door      & 1                                             & 3                                             & 0                                             & 0      & 0     & 0      \\
            Bank Vault       & 1                                             & 7                                             & 0                                             & 0      & 0     & 0      \\
            Light Switch     & 0                                             & 0                                             & 2                                             & 1      & 0     & 0      \\
            Traffic Light    & 0                                             & 0                                             & 3                                             & 3      & 0     & 0      \\
            Trust and Travel & 0                                             & 0                                             & 2                                             & 1      & 2     & 1      \\
            Rival Alliances  & 0                                             & 0                                             & 2                                             & 1      & 2     & 2      \\
            \midrule
            Rover Mission    & 1                                             & 3                                             & 4                                             & 2      & 2     & 1      \\
            \bottomrule
        \end{tabular}
    \end{center}

    \begin{block}{The Rover Mission composes three canonical patterns}
        \begin{itemize}
            \item Drill chain $=$ Locked Door unreachability, $(1, 3)$ on $\mathcal{I}_{\text{UR}}$
            \item Dish toggle $=$ Light Switch reversible mutex, $(2, 1)$ on $\mathcal{I}_{\text{MX}}$
            \item Orbit chain $=$ Trust and Travel irreversible mutex and one-directional sequencing, $(2, 1)$ on $\mathcal{I}_{\text{MX}}$ and $(2, 1)$ on $\mathcal{I}_{\text{GS}}$
            \item Per-measure counts add to $(1, 3, 4, 2, 2, 1)$
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S4.3.4, \S4.4}
\end{frame}

\begin{frame}[noframenumbering]{Backup, absolute counts instead of normalized measures}
    \hypertarget{bk:normalization}{}
    \begin{exampleblock}{Relative measures exist [Besnard and Grant, 2020]}
        Dividing by $|G|$, $|P|$, or the maximal conflict count would yield percentage-style scores comparable across problem sizes.
    \end{exampleblock}

    \begin{block}{Why the profiles stay absolute}
        \begin{itemize}
            \item Severity is not proportional to size, 2 conflicting goals among 100 may be easier to repair than a complete clique on 3
            \item Every denominator choice encodes an assumption about problem structure
            \item The profiles already carry relative information, scope against structure within each measure
            \item Future work calibrates normalization against the IPC benchmark distribution
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S4.6.2}
\end{frame}

%========================================================================
% Postulates and complexity (Thesis \S4.5)
%========================================================================

\begin{frame}[noframenumbering]{Backup, postulate choice and the combined measure}
    \hypertarget{bk:postulates}{}
    \begin{exampleblock}{Does the combined profile satisfy Planning Consistency?}
        \begin{itemize}
            \item No. \textit{3unsat} instances are unsolvable yet all six values stay zero at every horizon
            \item For $|G| \leq 2$ the converse holds, all measures zero implies solvable
        \end{itemize}
    \end{exampleblock}

    \begin{block}{Why these three postulates}
        \begin{itemize}
            \item Classical postulates assume formula structure, planning has goals, operators, and states
            \item Adding operators can resolve conflicts, so Monotonicity reverses sign
            \item Modified postulates and the multi-measure approach follow the ASP precedent [Ulbricht et al., 2016]
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S4.5.1, \S6.3.6, \S7.4}
\end{frame}

\begin{frame}[noframenumbering]{Backup, adding an operator can raise $\mathcal{I}_{\text{MX}}$ and $\mathcal{I}_{\text{GS}}$}
    \hypertarget{bk:op-mono-ce}{}
    \begin{exampleblock}{One construction breaks both, $P = \{a, g_1, g_2\}$, $I = \{a\}$, $G = \{g_1, g_2\}$}
        \footnotesize
        Start with $O = \{\,\textit{reach\_g1} = \langle \{a\}, \{g_1\}, \{a\}\rangle\,\}$, then add $\textit{reach\_g2} = \langle \{a\}, \{g_2\}, \{a\}\rangle$.\\[2pt]
        \centering
        \begin{tabular}{lcc}
            \toprule
                                      & before add & after add \\
            \midrule
            $\mathcal{I}_{\text{MX}}$ & $(0, 0)$   & $(2, 1)$  \\
            $\mathcal{I}_{\text{GS}}$ & $(0, 0)$   & $(2, 2)$  \\
            \bottomrule
        \end{tabular}
    \end{exampleblock}

    \begin{block}{Why the value rises, and why $\mathcal{I}_{\text{UR}}$ is immune}
        \footnotesize
        \begin{itemize}
            \item Before, $g_2$ is unreachable, so no mutex pair and no sequencing conflict can form
            \item The added operator makes $g_2$ reachable but deletes $a$, which nothing re-adds, so no reachable state holds both goals
            \item This operator-induced mutex, with neither order achievable, raises $\mathcal{I}$, while $\mathcal{I}_{\text{UR}}$ only falls as $S^h_{\text{reach}}$ grows
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S4.5.1}
\end{frame}

\begin{frame}[noframenumbering]{Backup, sequencing implies mutex}
    \hypertarget{bk:seq-implies-mx}{}
    \begin{exampleblock}{Proposition}
        For any $\Pi$ and horizon $h$, if $(g_1, g_2)$ is a sequencing conflict with $g_2$ achievable, then $\{g_1, g_2\}$ is a mutex pair at horizon $h$.
    \end{exampleblock}

    \begin{block}{Proof sketch and counterexample}
        \begin{itemize}
            \item Sequencing gives both individually achievable, and no trace has $g_1$ at $\sigma_i$ and $g_2$ at $\sigma_j$ with $i \leq j$
            \item The $i = j$ case is the mutex condition, no reachable state holds both
            \item Combined with achievability, this is the mutex definition
            \item On the Rover the sequencing pair $(\text{in\_orbit}, \text{mapped})$ is also one of the two mutex pairs, as the proposition predicts
            \item Converse fails on the dish toggle, $\{\text{aimed\_earth}, \text{aimed\_probe}\}$ is mutex yet reversible, so neither order is a sequencing conflict
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S4.5.2}
\end{frame}

\begin{frame}[noframenumbering]{Backup, PSPACE-completeness proof sketch}
    \hypertarget{bk:pspace-proof}{}
    \begin{block}{Membership of $\text{UPPER}_{\mathcal{I}_{\text{UR}}}$ in PSPACE}
        \footnotesize
        \begin{itemize}
            \item Guess an operator trace from $I$ in polynomial space, accept when $g$ appears
            \item NPSPACE $=$ PSPACE by Savitch, iterating over $|G|$ goals stays polynomial
        \end{itemize}
    \end{block}

    \begin{block}{Hardness via reduction from PlanSat}
        \footnotesize
        From $\Pi = \langle P, O, I, G \rangle$, build $\Pi' = \langle P \cup \{g^*\},\ O \cup \{o^*\},\ I,\ \{g^*\} \rangle$ with $o^* = \langle G, \{g^*\}, \emptyset \rangle$. Then $g^*$ is reachable iff $\Pi$ has a plan, so $\text{UPPER}_{\mathcal{I}_{\text{UR}}}$ at $k = 0$ decides PlanSat.
    \end{block}

    \begin{block}{Extends to MX, GS, and the other problems}
        \footnotesize
        \begin{itemize}
            \item For $\mathcal{I}_{\text{MX}}$ and $\mathcal{I}_{\text{GS}}$ a similar reduction makes the only path to $g_2$ delete $g_1$, so the pair is mutex or sequenced iff $\Pi$ is unsolvable
            \item LOWER is the complement of UPPER, EXACT checks both bounds, VALUE binary-searches UPPER, all staying in PSPACE or FPSPACE
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S4.5.3}
\end{frame}

\begin{frame}[noframenumbering]{Backup, PSPACE flattens the propositional multi-tier landscape}
    \hypertarget{bk:pspace-compare}{}
    \begin{exampleblock}{Decision-problem complexity, planning versus propositional $\mathcal{I}_c$}
        \centering
        \footnotesize
        \begin{tabular}{lll}
            \toprule
            Problem & Planning measures & Propositional $\mathcal{I}_c$ [Thimm and Wallner, 2016] \\
            \midrule
            UPPER   & PSPACE-complete   & NP-complete                                             \\
            LOWER   & PSPACE-complete   & coNP-complete                                           \\
            EXACT   & PSPACE-complete   & $D^p$-complete                                          \\
            VALUE   & FPSPACE           & FP$^{\text{NP}[\log n]}$-complete                       \\
            \bottomrule
        \end{tabular}
    \end{exampleblock}

    \begin{block}{Why the tiers collapse}
        \footnotesize
        \begin{itemize}
            \item Propositionally the base problem is satisfiability, NP-complete, so the measures stack into tiers above it
            \item For planning the base is PlanSat, already PSPACE-complete, a far larger ceiling
            \item PSPACE $=$ co-PSPACE collapses UPPER, LOWER, and EXACT, VALUE follows by polynomially many calls, so the gaps between measures vanish
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S4.5.3}
\end{frame}

%========================================================================
% Implementation (Thesis \S5)
%========================================================================

\begin{frame}[noframenumbering]{Backup, why ASP over SAT and MaxSAT}
    \hypertarget{bk:asp-vs-sat}{}
    \begin{block}{Reasons in favor of ASP}
        \begin{itemize}
            \item Default negation supports non-monotonic reasoning, matching reversed Operator Monotonicity
            \item Brave reasoning unions answer-set traces in one solver call, no manual enumeration
            \item ASP outperforms basic SAT encodings for inconsistency measures [Kuhlmann et al., 2025]
        \end{itemize}
    \end{block}

    \begin{block}{Trade-off acknowledged}
        Specialized MaxSAT beats ASP on classical formula-counting measures [Niskanen et al., 2023], and remains a future-work direction for scalability on planning-native measures.
    \end{block}

    \sectioncite{Thesis \S5, \S7.4}
\end{frame}

\begin{frame}[noframenumbering,fragile]{Backup, state-space exploration encoding}
    \hypertarget{bk:asp-listing}{}
    \begin{lstlisting}[style=asp]
time(0..horizon).
holds(P, 0) :- init(P).
% Applicability, positive and negative preconditions
neg_precond_violated(O, T) :- neg_precond(O, P), holds(P, T).
applicable(O, T) :- operator(O), time(T), T < horizon,
    not neg_precond_violated(O, T), holds(P, T) : precond(O, P).
% One operator per step, each choice a distinct trace
{ apply(O, T) : applicable(O, T) } 1 :- time(T), T < horizon.
% State transition with frame axiom
deleted(P, T) :- apply(O, T), delete(O, P).
holds(P, T+1) :- apply(O, T), add(O, P).
holds(P, T+1) :- holds(P, T), not deleted(P, T), time(T), T < horizon.
    \end{lstlisting}
    The choice rule branches into one answer set per execution trace. The frame axiom carries propositions forward unless an operator deletes them.

    \sectioncite{Thesis \S5.3.2}
\end{frame}

\begin{frame}[noframenumbering]{Backup, 80 automated tests verify the pipeline}
    \hypertarget{bk:validation}{}
    \begin{exampleblock}{Test suite}
        \begin{itemize}
            \item 80 tests across 8 categories, full suite runs in under one second
            \item 11 test scenarios match expected profiles exactly
            \item Categories cover scenarios, hierarchy, library API, end-to-end PDDL
        \end{itemize}
    \end{exampleblock}

    \begin{block}{Sample expected vs computed matches}
        \centering
        \scriptsize
        \begin{tabular}{lll}
            \toprule
            Category          & Scenario              & Profile              \\
            \midrule
            P1 Unreachability & Locked Door           & $(1, 3, 0, 0, 0, 0)$ \\
            P2 Mutex          & Traffic Light         & $(0, 0, 3, 3, 0, 0)$ \\
            P3 Sequencing     & Rival Alliances       & $(0, 0, 2, 1, 2, 2)$ \\
            Edge case         & Empty Goals           & $(0, 0, 0, 0, 0, 0)$ \\
            Edge case         & Negative Precondition & $(1, 1, 0, 0, 0, 0)$ \\
            \bottomrule
        \end{tabular}
    \end{block}

    \sectioncite{Thesis \S5.6}
\end{frame}

%========================================================================
% Evaluation (Thesis \S6)
%========================================================================

\begin{frame}[noframenumbering]{Backup, per-domain results breakdown}
    \hypertarget{bk:per-domain}{}
    \begin{center}
        \scriptsize
        \begin{tabular}{lcll}
            \toprule
            Domain              & OK / total & Classification         & Notable pattern                              \\
            \midrule
            diagnosis           & 18 / 20    & all Cat 2a             & 2 show $h{=}20$ mutex artifacts, UR 14--88\% \\
            3unsat              & 20 / 30    & all consistent         & $k$-tuple blind spot, horizon-independent    \\
            bottleneck          & 12 / 25    & 10 Cat 2c-seq, 2 cons. & MX varies with bottleneck position           \\
            cave-diving         & 9 / 25     & 1 Cat 2a, 8 cons.      & global constraint outside pairwise scope     \\
            pegsol-row5         & 3 / 15     & all Cat 2a             & one unreachable goal each                    \\
            document-transfer   & 3 / 20     & all Cat 2a             & up to 19 of 19 goals unreachable             \\
            chessboard-pebbling & 3 / 23     & all Cat 2c-seq         & identical $(3, 2, 3, 2)$ regardless of grid  \\
            \bottomrule
        \end{tabular}
    \end{center}

    \sectioncite{Thesis \S6.2}
\end{frame}

\begin{frame}[noframenumbering]{Backup, 3unsat and the $k$-tuple blind spot}
    \hypertarget{bk:3unsat}{}
    \begin{exampleblock}{Pairwise coexistence with no triple-satisfying state}
        \[
            G = \{g_1, g_2, g_3\}, \qquad
            S_{\text{reach}} \supseteq \{\{g_1, g_2\}, \{g_2, g_3\}, \{g_1, g_3\}\}
        \]
        Every pair is reachable together, yet no state satisfies all three.
    \end{exampleblock}

    \begin{block}{Consequence for the measures}
        \begin{itemize}
            \item All goals individually reachable, so $\mathcal{I}_{\text{UR}}(\Pi) = (0, 0)$
            \item No mutex pair exists in $S_{\text{reach}}$, so $\mathcal{I}_{\text{MX}}(\Pi) = (0, 0)$
            \item No ordered sequencing failure either, so $\mathcal{I}_{\text{GS}}(\Pi) = (0, 0)$
            \item All 20 completed \textit{3unsat} instances confirm this at every tested horizon
            \item Future work extends to $k$-tuple analysis for $|G| \geq 3$ failures
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S6.3.6, \S7.4}
\end{frame}

\begin{frame}[noframenumbering]{Backup, horizon sensitivity}
    \hypertarget{bk:horizon}{}
    \begin{exampleblock}{Solvable cave-diving \texttt{satprob01} across horizons}
        \begin{itemize}
            \item At $h = 10$, $\mathcal{I}_{\text{MX}}(\Pi) = (3, 2)$ and $\mathcal{I}_{\text{GS}}(\Pi) = (3, 4)$, classified 2c-sequencing
            \item At $h = 20$, every profile is zero, classified consistent
        \end{itemize}
    \end{exampleblock}

    \begin{block}{Soundness-coverage tradeoff}
        \begin{itemize}
            \item Short $h$ misses long witness traces, producing spurious conflicts
            \item Identical artifacts appear on solvable and unsolvable cave-diving instances at $h = 10$
            \item $h = 20$ chosen because no observed solvable instance produces conflicts
            \item Longer $h$ adds grounding cost and reduces coverage
            \item Same $h$-parameterization as $\text{LTL}_{\text{ff}}$ measures [Corea et al., 2024; Kuhlmann, 2025]
        \end{itemize}
    \end{block}

    \sectioncite{Thesis \S6.4}
\end{frame}
